export type NoteNavigationType = {
  id: string | number;
  title: string;
  created: string;
  updated: string;
  summary: string;
  count: number;
};

export type NoteContentItemType = {
  id: string | number;
  content: string;
  created: string;
  updated: string;
  summary: string;
};
